# ─── Core API Framework ───────────────────────────────────────
fastapi==0.111.0          # The web framework (like Flask but faster + auto docs)
uvicorn[standard]==0.29.0 # The server that runs FastAPI

# ─── Database ─────────────────────────────────────────────────
sqlalchemy==2.0.30        # ORM: write Python, get SQL
alembic==1.13.1           # Database migrations (like git for your DB schema)
psycopg2-binary==2.9.9    # PostgreSQL driver
# For local dev with SQLite, no extra driver needed

# ─── Auth & Security ──────────────────────────────────────────
python-jose[cryptography]==3.3.0   # JWT tokens (login sessions)
passlib[bcrypt]==1.7.4             # Password / PIN hashing
python-multipart==0.0.9            # Form data parsing

# ─── HTTP Client (for calling mobile money APIs) ──────────────
httpx==0.27.0             # async HTTP client (like requests but async)

# ─── Config & Environment ─────────────────────────────────────
pydantic-settings==2.2.1  # Load config from .env file
python-dotenv==1.0.1      # Read .env files

# ─── Background Jobs ──────────────────────────────────────────
apscheduler==3.10.4       # Scheduled tasks (e.g. mark overdue loans daily)

# ─── Validation ───────────────────────────────────────────────
pydantic==2.7.1           # Data validation (comes with FastAPI, pinned here)

# ─── Testing ──────────────────────────────────────────────────
pytest==8.2.0
pytest-asyncio==0.23.6
httpx==0.27.0             # also used to test API endpoints
